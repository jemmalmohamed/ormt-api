package ma.org.ormt.modules.indicateurs.indicateur.association.repository;

import ma.org.ormt.core.commun.base.repository.BaseRepository;
import ma.org.ormt.modules.indicateurs.indicateur.models.IndicateurDimension;

public interface IndicateurDimensionRepository extends BaseRepository<IndicateurDimension> {

    // List<IndicateurDimension> findByAvionId(Long avionId);

    // List<IndicateurDimension> findByCapteurId(Long capteurId);

    // Optional<IndicateurDimension> findByAvionIdAndCapteurId(Long avionId, Long
    // capteurId);

    // Boolean existsByAvionIdAndCapteurId(Long avionId, Long capteurId);

    // Boolean existsByAvionId(Long avionId);

    // Boolean existsByCapteurId(Long capteurId);

}