services:
  minio-ormt:
    image: minio/minio:latest
    container_name: minio-ormt
    restart: always
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      # Better security defaults
      MINIO_PROMETHEUS_AUTH_TYPE: "public"
      MINIO_BROWSER: "on"
      MINIO_DOMAIN: ${MINIO_API_HOST_URL:-minio.local}
    # Command to start Minio server and expose console on port 9001
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.minio-ormt.entrypoints=web"
      - "traefik.http.routers.minio-ormt.rule=Host(`${MINIO_CONSOLE_HOST_URL}`)"
      - "traefik.http.services.minio-ormt.loadbalancer.server.port=${MINIO_CONSOLE_PORT}"
      - "traefik.http.services.minio-ormt.loadbalancer.server.port=${MINIO_API_PORT}"

    healthcheck:
      # Use curl to check the Minio health endpoint
      test: [ "CMD", "curl", "-f", "http://localhost:9000/minio/health/live" ]
      interval: 30s
      timeout: 20s
      retries: 3
      start_period: 30s # Allow time for Minio to initialize
    networks:
      - proxy

volumes:
  minio_data:
    # Define the volume for Minio data persistence

networks:
  proxy:
    external: true
